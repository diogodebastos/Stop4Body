{
  "cuts":[
    {
      "name":"PreselectionMVA",
      "expression":"(isPrompt == 0) && (HT > 200) && (Met > 280) && (Jet1Pt > 110)",
      "latex":"Preselection MVA"
    },
    {
      "name":"PreselectionMVA_lep30",
      "expression":"(isPrompt == 0) && (HT > 200) && (Met > 280) && (Jet1Pt > 110) && (LepPt < 30)",
      "latex":"Preselection MVA (p_{T}(l) < 30)"
    },
    {
      "name":"SelectionMVA",
      "expression":"(BDT > 0.31) && (isPrompt == 0) && (HT > 200) && (Met > 280) && (Jet1Pt > 110)",
      "latex":"Selection MVA"
    },
    {
      "name":"SelectionMVA_lep30",
      "expression":"(BDT > 0.31) && (isPrompt == 0) && (HT > 200) && (Met > 280) && (Jet1Pt > 110) && (LepPt < 30)",
      "latex":"Selection MVA (p_{T}(l) < 30)"
    }
  ],
  "variables":[
    {
      "expression":"LepPt",
      "label":"p_{T}(l)",
      "legPos":"top",
      "max":35.0,
      "bins":20
    },
    {
      "name":"LepPt_ext",
      "expression":"LepPt",
      "label":"p_{T}(l)",
      "legPos":"top",
      "max":105.0,
      "bins":30
    }
  ],
  "2Dplots":[
  ]
}
